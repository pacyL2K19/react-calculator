import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';

const Calculator = () => (
  <div>Calculator</div>
);

ReactDOM.render(
  <Calculator />,
  document.getElementById('root'),
);
